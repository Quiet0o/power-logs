services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATABASE_URL=/app/data/sqlite.db
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
